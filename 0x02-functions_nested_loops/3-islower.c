#include "main.h"

/**
 * _islower - check if char is lower
 *
 * Return: 1 if lower else is 0
 */
int _islower(int c)
{
	c = 'H';
	c = '0';
	c = 'o';
	c = 'c';
	c = 'I';
	c = 's';

	if(_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
